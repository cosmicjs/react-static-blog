import React from 'react'

export default () => (
  <div className="container">
    <p>React Static is a progressive static site generator for React.</p>
    <p>Cosmic JS is Headless CMS API and toolkit to build modern apps faster,together.</p>
    <p>This application uses both React-Static and Cosmic JS to bulid next generation JAMStack apps.</p>
  </div>
)
